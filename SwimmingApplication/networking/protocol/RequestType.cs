namespace networking.protocol;

public enum RequestType
{
    LOGIN,
    LOGOUT,
    REGISTER,
    GET_RACES,
    GET_PARTICIPANTS,
    GET_REGISTRATIONS,
    GET_USERS,
    ADD_REGISTRATION,
    GET_PARTICIPNT_BY_ID,
    GET_RACE_BY_ID
}